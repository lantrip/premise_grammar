; Outline items for code navigation
; Provides hierarchical document structure in editor outline view

; Story structure headers
(act_header) @item
(scene_header) @item  
(cel_header) @item

; File headers (TITLE:, WORLD:, etc.)
(file_header) @item

; Entity definitions
(entity_definition) @item

; Entity blocks
(entity_block_start) @item

; Import statements
(import_statement) @item

; Adapter statements  
(adapter_statement) @item

; Metadata lines
(metadata_line) @item